# SkillKit generated files (legacy — no longer used, but kept in case of re-run)
.codex/
.claude/
AGENTS.md
skills-lock.json
.skills
.skillkit/

# Vendored third-party skills — local only, never pushed.
# Provenance lives in SOURCES.toml (tracked).
vendor/

# Local app state, not catalogue content
.saggar/

# Python bytecode
__pycache__/
*.pyc
