# Skills deployed to global targets (~/.agents/skills, ~/.codex/skills) by
# default. Everything in canonical NOT listed here is available only via a
# project's `add`.
#
# This is the CURATED SET after Phase 2 reclassification (skills-2gs).
# Niche, dead-mechanism, experimental, and vendored skills remain in-repo
# and are available via a project's `add`, but are not shipped globally.
#
# NOTE: adding a new skill to canonical requires adding it here too, or it
# won't deploy globally. Run reconcile/deploy --dry-run to catch drift.

# 18 skills (tools, VCS, search, personal)
skills = [
    "almanac",
    "bin-creator",
    "backlog",
    "br",
    "cbm",
    "cheatsheets",
    "code-and-docs-search",
    "file-introspection",
    "filesearch",
    "git-change-manage",
    "git-vcs",
    "jj-change-manage",
    "jj-vcs",
    "lark-crm",
    "mcp-manage",
    "op-env-wrap-tool",
    "root-cause-debugger",
    "skills-manage",
    "zcode",
]
